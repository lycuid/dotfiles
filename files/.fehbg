#!/bin/sh

# prioritize xdg user dirs, if exists.
[ -f "$XDG_CONFIG_HOME/user-dirs.dirs" ] && \
  . $XDG_CONFIG_HOME/user-dirs.dirs

basedir=${XDG_PICTURES_DIR:-$HOME/Pictures}

ls $basedir \
  | shuf \
  | head -n 1 \
  | xargs -i feh --no-fehbg --bg-fill $basedir/{}

